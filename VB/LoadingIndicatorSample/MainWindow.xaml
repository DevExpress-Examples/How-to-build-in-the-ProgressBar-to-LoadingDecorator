<Window x:Class="LoadingIndicatorSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
        xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
        xmlns:local="clr-namespace:LoadingIndicatorSample"
        xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <dx:LoadingDecorator Grid.Row="1" Name="ld">
            <dx:LoadingDecorator.SplashScreenDataContext>
                <local:LoadingScreenViewModel />
            </dx:LoadingDecorator.SplashScreenDataContext>
            <dx:LoadingDecorator.SplashScreenTemplate>
                <DataTemplate>
                    <dxe:ProgressBarEdit Width="200" EditValue="{Binding Progress}" />
                </DataTemplate>
            </dx:LoadingDecorator.SplashScreenTemplate>

            <dxg:GridControl></dxg:GridControl>
        </dx:LoadingDecorator>
    </Grid>
</Window>
